name: CI Pipeline

on:
  push:
    branches: [main, cicd]

jobs:
  test:
    name: Run RSpec Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: test_db
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      RAILS_ENV: test
      DATABASE_URL: postgresql://postgres:password@localhost:5433/test_db
      DEVISE_JWT_SECRET_KEY: "test_jwt_secret_1234567890"

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4.5
          bundler-cache: true

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Set up Node & Yarn
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install JS dependencies
        run: yarn install --check-files

      - name: Wait for Postgres
        run: |
          echo "Waiting for Postgres on localhost:5433..."
          until pg_isready -h localhost -p 5433 -U postgres; do
            sleep 3
          done

      - name: Prepare test database
        run: bundle exec rails db:create db:schema:load

      - name: Run RSpec tests
        id: tests
        run: bundle exec rspec --format documentation --color

      - name: Generate CI Summary
        if: always()
        run: |
          echo "### ✅ CI Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment:** Ruby 3.4.5 / Node 20" >> $GITHUB_STEP_SUMMARY
          echo "- **Database:** PostgreSQL 15 (Docker Service)" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** ${GITHUB_JOB}" >> $GITHUB_STEP_SUMMARY
          echo "- **Test Results:** All RSpec tests completed successfully." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✔️ Continuous Integration checks passed and ready for deployment." >> $GITHUB_STEP_SUMMARY
